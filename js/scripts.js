// function powerOfTwo(number) {
//   if(!isNaN(number)) {
//     return number * number;
//   } else {
//     powerOfTwo(parseInt(Prompt("that wasnt a number! what number?", '2')));
//   }
// };
//
// let answer = powerOfTwo(parseInt(prompt("what number do you want to square?", '2')));
// alert(answer);

function parseString(string) {
  let parsedLetter = string.charAt(0).toUpperCase();
  let stringArr = string.split('');
  stringArr.splice(0,1,parseLetter);
  string = stringArr.join('');
  if(string.charAt(string.length - 1))
}
